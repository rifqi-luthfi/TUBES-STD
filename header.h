#ifndef HEADER_H_INCLUED
#define HEADER_H_INCLUED

#include <iostream>
#include <conio.h>
#include <stdlib.h>
#include <windows.h>
#include <ctime>
#include <time.h>

using namespace std;

#define first(L) L.first
#define last(L) L.last
#define info(P) P->Data
#define prev(P) P->prev
#define next(P) P->next

#define nextUser(P)P->nextUser
#define nextBarang(P)P->nextBarang

#endif